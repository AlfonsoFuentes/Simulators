<!-- EquipmentResultsList.razor -->
<MudExpansionPanels>
    @foreach (var equipmentResult in AnalysisResults)
    {
        <EquipmentResultItem EquipmentResult="equipmentResult" />
    }
</MudExpansionPanels>

@code {
    [Parameter]
    public List<EquipmentAnalysisResult> AnalysisResults { get; set; } = new();
}